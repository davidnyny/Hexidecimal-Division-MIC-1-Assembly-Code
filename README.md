# Hexidecimal-Division-MIC-1-Assembly-Code
Accept two integer values (in HEX) from the keyboard using two different prompt messages. Given two integer values in HEX, compute both quotient and remainder.
